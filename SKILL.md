---
name: jimavlog
description: Use when user wants to distill a short video (Douyin/TikTok/Kuaishou) creator's style — extract 4-layer expression DNA and produce a reusable AI skill for generating on-brand scripts. Trigger on requests such as "蒸馏这个博主" "分析他的风格" "学这个博主的表达".
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [content-creation, short-video, douyin, distillation, style-transfer]
    related_skills: [academic-narratologist, marketing-douyin-strategist]
---

# JimaVlog — 短视频博主风格蒸馏器

> *"换任何话题，风格都不崩。"*

## Overview

JimaVlog 蒸馏一个短视频博主的**表达操作系统**——不只是模仿表面特征（口头禅、句式），而是提取四层递进的基因结构。输出产物是可安装的 Hermes Skill，后续用户输入任何话题，AI 都会用这个博主的认知框架 + 说服策略 + 话步编排 + 语言指纹来生成文案。

**核心理念**：底层世界观决定论证方式，论证方式决定段落编排，段落编排最终落地为字词句。换了话题，只要底层框架不变，风格就不会崩。

### 与同类方案的区别

| 方案 | 蒸什么 | 换话题崩不崩 | 输出形式 |
|------|--------|------------|---------|
| blogger-distiller | 内容打法(认知·策略·内容) | ⚠️ 可能崩 | HTML报告 + Skill |
| nuwa-skill | 心智模型(任何人都能蒸馏) | ✅ 不崩 | 对话人格Skill |
| **JimaVlog** | **短视频口播特化(四层+质量门控)** | ✅ 不崩 | Script生成Skill |

## When to Use

- 用户想学习某个抖音/快手博主的表达风格来生成自己的视频文案
- 用户给了博主主页链接或录屏，要求「蒸馏」
- 用户说「分析这个博主的风格」「学他的表达方式」
- 用户要在不同话题之间保持风格一致性

**Don't use for:**
- 分析小红书/B站图文博主（用 blogger-distiller）
- 蒸馏名人决策思维而非短视频风格（用 nuwa-skill）
- 只需要一次性的「仿写一条」（直接用 LLM few-shot）

## 前置条件

- 目标博主的视频文案数据（20-50条，来源不限：手动复制 / 录屏+视觉AI / TikHub API / TikTokDownloader）
- LLM API（DeepSeek 推荐，成本 ¥0.01/次蒸馏）

## 蒸馏流程

```
📥 输入                🔬 第一轮LLM              🛡 质量过滤             🎯 第二轮LLM            ✅ 验证
20-50条文案      →    四层结构分析           →   三重验证             →   Skill生成          →   盲测
                       │                        │                      │                       │
                  analysis_raw.json       analysis_filtered.json   {博主}_style.skill/    相似度评分
```

---

## 一、四层蒸馏框架

### Layer 4 — 认知框架（可迁移性的锚）

**回答什么**：博主用什么思维模型理解世界？

不满足于描述「TA 相信反共识」，而是**归类为已知框架类型**：

| 框架类型 | 识别特征 | 典型句式 |
|---------|---------|---------|
| 博弈论框架 | 所有问题归因为利益博弈/信息差 | "一切XX本质都是YY博弈" |
| 第一性原理框架 | 回到事物本质重新推导 | "回到XX的本质" |
| 二元对立框架 | 把人/事分成两类来制造认知势差 | "XX的人分两种，一种YY，一种ZZ" |
| 时间折叠框架 | 用时间跨度制造洞察感 | "3年前XX，现在YY，3年后ZZ" |
| 层级升维框架 | 在不同抽象层级之间切换 | "在战术层面看XX，在战略层面看YY" |
| 反常识框架 | 以推翻大众认知来建立权威 | "你以为XX，其实YY" |

**操作**：分析每条视频结尾的「结论句」，统计各框架的出现频率。取 TOP2 为主要框架。

**完成标准**：至少识别出 2 个框架，每个有 3+ 条视频示例支撑。

---

### Layer 3 — 说服策略（情感的配方）

**回答什么**：博主靠什么让观众信服？

量化 Ethos（身份信用）、Pathos（情感触发）、Logos（逻辑证据）的使用比例和位置：

```
统计维度：
  Ethos: "我干这行20年" / "我见过太多案例" / 身份信号
  Pathos: 制造焦虑/希望/共情/愤怒 的表达
  Logos: 数据/案例/逻辑推导

产出：
  - 总比例：Ethos X% / Pathos Y% / Logos Z%
  - 位置规律：开头多用什么？主体用什么？结尾用什么？
  - 示例：张三 → 开头Pathos(共情) 40% → 主体Logos(案例) 30% → 结尾Ethos(信用) 30%
```

**完成标准**：三种诉诸各统计 ≥ 5 条视频，位置规律在 ≥ 70% 视频中一致。

---

### Layer 2 — 话步原型（60秒怎么走）

**回答什么**：每段做什么动作？动作的顺序和占比是什么？

拆解每条视频为话步序列（Move-Step），然后聚类合并为高频原型：

```
话步类型库：
  钩子、背景、冲突、痛点、误判、本质、案例、方案、升华、CTA

产出：
  - 高频话步序列：[钩子→痛点→误判→本质→方案→CTA]  出现率 65%
  - 此话步序列中每步的平均字数占比
  - 其他变体序列及其出现率
```

**操作**：逐条标注话步 → 用 LLM 聚类同类序列 → 取 TOP3 高频序列。

**完成标准**：TOP1 话步序列至少在 30% 视频中出现（20条中 ≥ 6条）。

---

### Layer 1 — 语言指纹（听起来像不像）

**回答什么**：可量化的文本特征参数。

| 参数 | 计算方式 | 用途 |
|------|---------|------|
| 平均句长 | 按标点切句，统计中文字数均值 | 控制节奏密度 |
| 句长标准差 | 长句和短句的交替幅度 | 控制节奏波动 |
| 反问频率 | 反问句数 / 总句数 | 控制互动感 |
| 排比频率 | 排比结构数 / 总段落数 | 控制气势感 |
| 高频词 TOP20 | 去除停用词后的词频统计 | 注入标志性词汇 |
| 口语化程度 | 语气词 & 口语连接词密度 | 控制亲切感 |

**完成标准**：6 项参数全部产出，每项基于 ≥ 20 条视频计算。

---

## 二、质量门控

在 L4-L1 结构分析完成后，不直接进入 Skill 生成。先过三道门：

### Gate 1：频率门

```
话步原型必须在 ≥ 30% 视频中出现（严格模式）/ ≥ 20%（宽松模式）
```

低于阈值的模式标记为「疑似」，不进入 Skill 核心配方，放入「创作禁区」备注。

### Gate 2：区分度门

```
认知框架必须对「两个不同博主」产生不同的分类结果。
```

如果用户蒸馏了博主 A 和博主 B，两个都被归为「博弈论框架」——回到 L4 重新分析，寻找更细粒度的区分特征。

**操作**：蒸馏新博主后，与已有风格库中所有博主比对 L4 框架。碰撞时 Deepen——增加次级框架标签（如「博弈论·零和型」vs「博弈论·合作型」）。

### Gate 3：置信度标注

每个输出维度标注置信度等级：

| 置信度 | 条件 | 标注 |
|--------|------|------|
| 高 | 在 ≥ 70% 视频中一致出现 | ✅ 确定 |
| 中 | 在 40-69% 视频中一致出现 | ⚠️ 倾向 |
| 低 | 在 20-39% 视频中出现 | 🔍 疑似 |

**完成标准**：L4-L1 所有维度均完成置信度标注，≥ 60% 维度为「高」或「中」置信度。

---

## 三、Skill 生成

质量门通过后，进入第二轮 LLM 调用——生成可安装的 Hermes Skill：

### 输出结构

```
{博主名}_style.skill/
├── SKILL.md          # 主 Skill 文件
└── references/
    └── examples.md   # 原始文案摘要 + 标注示例
```

### SKILL.md 包含章节

| 章节 | 内容 | 来源层 |
|------|------|--------|
| 使用说明 | 此 Skill 怎么用，什么场景有效 | — |
| 认知框架速查 | 底层思维模型 + 框架类型 + 示例 | L4 |
| 说服配方 | Ethos/Pathos/Logos 配比 + 位置 | L3 |
| 话步模板 | 高频话步序列 + 每步指令 | L2 |
| 语言参数 | 句长/反问率/高频词等量化参数 | L1 |
| 创作禁区 | 博主绝对不会做的事（基于缺失模式） | 全层 |
| 对比示例 | 正确模仿 vs 错误模仿 各 2 例 | 全层 |
| 选题灵感 | 10 个适合此风格的话题方向 | — |
| 自检清单 | 生成后自我检查的 5 个问题 | — |

### Few-Shot 嵌入

Skill 的 System Prompt 必须嵌入 2-3 条目标博主的真实文案作为 Few-Shot 锚定，防止 LLM 在生成时偏离风格。

**完成标准**：SKILL.md ≤ 15,000 字符，通过 `skill_view` 可加载。

---

## 四、验证

Skill 生成后，进行盲测：

```
① 选 3 个目标博主从未涉及过的话题领域
② 用生成的 Skill 创建 3 条文案
③ 人工或 LLM 评分（1-5分）：
   - 像不像：风格相似度 ≥ 4/5 通过
   - 换话题后是否仍然成立
   - 是否出现「强行套口头禅」的机械模仿
```

任意一条评分 < 4 → 回到蒸馏流程，检查：L4 框架是否归错类？L2 话步原型是否漏了关键步骤？

**完成标准**：3 条均 ≥ 4 分。

---

## Common Pitfalls

1. **把表面特征当核心**：口头禅、固定句式只是 L1 的表象。如果只有 L1 没有 L4，换话题必崩。

2. **样本太少**：≤ 15 条视频时，L2 话步原型不可靠，L3 说服策略比例波动大。硬性要求 ≥ 20 条。

3. **框架归类过于笼统**：两个不同博主都归为「博弈论框架」时，要进入次级分类，否则 Skill 没有区分度。

4. **跳过验证**：盲测是唯一能发现「看起来像但实际不像」的方式。不要因为分析报告好看就跳过。

5. **混淆博主风格和平台算法偏好**：博主使用某类开头可能是因为平台算法奖励它，不一定是博主的固有风格。话步原型应标注哪些步骤可能是算法驱动的。

---

## Verification Checklist

- [ ] 20-50 条视频文案已输入
- [ ] L4 认知框架：≥ 2 个框架类型，每个 ≥ 3 条例证
- [ ] L3 说服策略：Ethos/Pathos/Logos 比例 + 位置规律已产出
- [ ] L2 话步原型：TOP1 序列在 ≥ 30% 视频中出现
- [ ] L1 语言指纹：6 项参数全部产出
- [ ] Gate 1 频率门：低频模式已标记为「疑似」
- [ ] Gate 2 区分度门：与已有风格库无碰撞或已 Deepen
- [ ] Gate 3 置信度：全维度已标注
- [ ] Skill 生成：SKILL.md ≤ 15,000 字符，通过 `skill_view` 加载
- [ ] 盲测通过：3 条跨领域文案均 ≥ 4 分
